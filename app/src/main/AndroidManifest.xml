<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.FLASHLIGHT" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" tools:ignore="QueryAllPackagesPermission" />
    <uses-permission android:name="android.permission.WRITE_SECURE_SETTINGS" tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.WRITE_SETTINGS" tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="com.android.launcher.permission.INSTALL_SHORTCUT" />
    
    <!-- Required for Android 11+ to discover Icon Packs and All Apps -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
        <intent>
            <action android:name="com.novalauncher.THEME" />
        </intent>
        <intent>
            <action android:name="org.adw.launcher.THEMES" />
        </intent>
        <intent>
            <action android:name="com.gau.go.launcherex.theme" />
        </intent>
        <provider android:authorities="moe.shizuku.server.shizuku" />
    </queries>

    <application
        android:name=".SidePanelApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SidePanel.M3"
        android:resizeableActivity="true"
        tools:targetApi="31">

        <!-- Entry point is provided by the LauncherHidden / LauncherVisible aliases below,
             so the launcher entry can be swapped at runtime to toggle Recents visibility. -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <!-- Default entry (matches hideFromRecents default = true): task hidden from Recents. -->
        <activity-alias
            android:name=".LauncherHidden"
            android:targetActivity=".MainActivity"
            android:exported="true"
            android:enabled="true"
            android:excludeFromRecents="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity-alias>

        <!-- Alternate entry used when the user disables "Hide from Recents". -->
        <activity-alias
            android:name=".LauncherVisible"
            android:targetActivity=".MainActivity"
            android:exported="true"
            android:enabled="false">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity-alias>

        <activity
            android:name=".SetupActivity"
            android:exported="false"
            android:theme="@style/Theme.SidePanel.M3" />

        <activity
            android:name=".ToggleActivity"
            android:label="@string/label_toggle_sidebar"
            android:icon="@mipmap/ic_launcher"
            android:theme="@style/Theme.Transparent"
            android:excludeFromRecents="true"
            android:taskAffinity=""
            android:launchMode="singleInstance"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.CREATE_SHORTCUT" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.SHORTCUT" />
            </intent-filter>
        </activity>

        <activity
            android:name=".SettingsMainActivity"
            android:label="@string/settings"
            android:theme="@style/Theme.SidePanel.M3" />

        <activity
            android:name=".AppearanceSettingsActivity"
            android:label="@string/section_appearance"
            android:theme="@style/Theme.SidePanel.M3" />

        <activity
            android:name=".InteractionSettingsActivity"
            android:label="@string/section_interaction"
            android:theme="@style/Theme.SidePanel.M3" />

        <activity
            android:name=".HandleSettingsActivity"
            android:label="@string/section_handle"
            android:theme="@style/Theme.SidePanel.M3" />

        <activity
            android:name=".ToolsSettingsActivity"
            android:label="@string/section_tools"
            android:theme="@style/Theme.SidePanel.M3" />

        <activity
            android:name=".SettingsActivity"
            android:label="@string/settings_title"
            android:theme="@style/Theme.SidePanel.M3" />

        <activity
            android:name=".SupportActivity"
            android:label="@string/support_dev_title"
            android:theme="@style/Theme.SidePanel.M3" />

        <activity
            android:name=".MiscellaneousSettingsActivity"
            android:label="@string/section_misc"
            android:theme="@style/Theme.SidePanel.M3" />

        <activity
            android:name=".IconPackActivity"
            android:label="@string/icon_pack"
            android:theme="@style/Theme.SidePanel" />



        <service
            android:name=".FloatingPanelService"
            android:enabled="true"
            android:exported="false"
            android:stopWithTask="false"
            android:foregroundServiceType="specialUse">
            <property android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Overlay for sidebar launcher and quick settings dashboard" />
        </service>

        <service
            android:name=".PanelAccessibilityService"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <service
            android:name=".NotificationTrackingService"
            android:label="@string/notif_access_label"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <service
            android:name=".PanelTileService"
            android:icon="@drawable/ic_tile_sidebar"
            android:label="@string/shortcut_toggle_short"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:multiprocess="false"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL" />

    </application>

</manifest>
